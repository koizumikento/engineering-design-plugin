interface:
  display_name: "Integration Check"
  short_description: "Check enclosure and PCB fit, clearances, connector alignment, and mounting consistency."
  default_prompt: "Use this skill to validate an approved integrated specification and produce a board-to-enclosure compatibility report."

policy:
  allow_implicit_invocation: true
