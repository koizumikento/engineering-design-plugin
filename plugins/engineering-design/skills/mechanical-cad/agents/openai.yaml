interface:
  display_name: "Mechanical CAD"
  short_description: "Build and validate build123d mechanical CAD."
  default_prompt: "Use $mechanical-cad for the requested modeling, export, or read-only geometry inspection, with evidence for the requested checks."
